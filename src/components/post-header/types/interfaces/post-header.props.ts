import PlaceModel from '@/types/interfaces/models/place.model'
import UserModel from '@/types/interfaces/models/user.model'

interface PostHeaderProps {
  user: UserModel
  place: PlaceModel
}

export default PostHeaderProps
